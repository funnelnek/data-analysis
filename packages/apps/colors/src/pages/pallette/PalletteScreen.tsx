import { FC, ReactElement } from "react";

interface Props {}

export const PalletteScreen: FC<Props> = (props: Props): ReactElement => {
    return (
        <div>
            
        </div>
    );
};