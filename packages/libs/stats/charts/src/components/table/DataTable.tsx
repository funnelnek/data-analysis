import React, { FC } from "react";
import { DataTableOptions } from './types';


export const DataTable: FC<DataTableOptions> = (props: DataTableOptions): JSX.Element => {
    return (
        <div></div>
    );
}