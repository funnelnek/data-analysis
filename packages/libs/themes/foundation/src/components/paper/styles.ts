import { SerializedStyles, css, FunctionInterpolation, Theme } from "@emotion/react";


export const paper = (styles: object): FunctionInterpolation<Theme> => {
    return (theme: Theme) => css({
        
    });
}